{
  "schema_version": "v1",
  "template_name": "deployment.yaml",
  "template_description": "Kubernetes deployment with configurable replicas, resources, and environment",
  "template_type": "kubernetes",
  "variables": [
    {
      "name": "app_name",
      "description": "Application name (used for deployment, labels, and container name)",
      "type": "string",
      "default": ""
    },
    {
      "name": "namespace",
      "description": "Kubernetes namespace to deploy to",
      "type": "string",
      "default": "default"
    },
    {
      "name": "environment",
      "description": "Environment label (dev, staging, production)",
      "type": "string",
      "default": "dev"
    },
    {
      "name": "replicas",
      "description": "Number of pod replicas",
      "type": "int",
      "default": "3"
    },
    {
      "name": "image",
      "description": "Container image (e.g., nginx:latest, myapp:v1.2.3)",
      "type": "string",
      "default": ""
    },
    {
      "name": "container_port",
      "description": "Container port to expose",
      "type": "int",
      "default": "8080"
    },
    {
      "name": "memory_request",
      "description": "Memory request (e.g., 256Mi, 1Gi)",
      "type": "string",
      "default": "256Mi"
    },
    {
      "name": "cpu_request",
      "description": "CPU request (e.g., 100m, 500m)",
      "type": "string",
      "default": "100m"
    },
    {
      "name": "memory_limit",
      "description": "Memory limit (e.g., 512Mi, 2Gi)",
      "type": "string",
      "default": "512Mi"
    },
    {
      "name": "cpu_limit",
      "description": "CPU limit (e.g., 500m, 1000m)",
      "type": "string",
      "default": "500m"
    }
  ]
}
