apiVersion: v1
kind: Service
metadata:
  name: {{ .app_name }}-service
  namespace: {{ .namespace }}
spec:
  selector:
    app: {{ .app_name }}
  ports:
  - port: {{ .service_port }}
    targetPort: {{ .container_port }}
  type: {{ .service_type }}
